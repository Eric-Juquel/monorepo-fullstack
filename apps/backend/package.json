{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "description": "NestJS backend — to be created",
  "scripts": {
    "dev": "echo 'NestJS backend not yet created'",
    "build": "echo 'NestJS backend not yet created'",
    "lint": "echo 'NestJS backend not yet created'",
    "test:run": "echo 'NestJS backend not yet created'"
  }
}
